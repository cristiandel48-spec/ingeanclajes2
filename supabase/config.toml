# Configuracion del proyecto para el CLI de Supabase.

project_id = "ktomujtqkitctdoznbmp"

# La verificacion de JWT del portal se deja ENCENDIDA (el valor por defecto).
# La aplicacion envia el token de sesion de quien esta usando el sistema, asi
# que pasa sin problema, y es una capa mas antes de llegar al codigo.
#
# Nota sobre el nombre: en Supabase la direccion de una funcion se fija al
# crearla y renombrarla en el panel no la cambia. Esta quedo publicada como
# smart-endpoint aunque en la lista se muestre como "gestionar-usuarios".
